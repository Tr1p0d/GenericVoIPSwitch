-module(core_test).

-include_lib("eunit/include/eunit.hrl").

%core_test() ->
%	generic_exchange_app:start(),
%	?assertMatch({ok, _}, generic_exchange_dialog_manager:create_dialog(self(), blabla, caller)).
%
%core_2_test() ->
%	?assertMatch({ok, _}, generic_exchange_dialog_manager:lookup_dialog({blablabla, blabla, caller})).

